# Pipeline de dados — Sistema de Roteirização de Visitas às Agências
# Python 3.10 ou superior
#
# Instalação:
#   pip install -r requirements.txt

pandas>=2.0            # leitura e tratamento das bases
openpyxl>=3.1          # leitura dos arquivos .xlsx do Bacen e da base consolidada
scikit-learn>=1.3      # k-means na formação das regionais
shapely>=2.0           # simplificação da geometria dos estados para o mapa
requests>=2.31         # download das bases auxiliares (etapa 01)
